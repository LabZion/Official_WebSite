apiVersion: v1
kind: Pod
metadata:
  labels:
    app: jnlp-slave
  name: jnlp-slave
spec:
  containers:
    - name: jnlp-container
      image: ninech/jnlp-slave-with-docker
      imagePullPolicy: IfNotPresent

    - name: "dind"
      image: docker:19.03.9-dind
      imagePullPolicy: IfNotPresent
      